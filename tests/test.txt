pkill -f erl    
# Terminal 1:
erl -sname server
     c(server).
     server:start().


# Terminal 2 (c1, ligado diretamente ao server):
erl -sname c1
     c(sensor).
     sensor:start(c1, [server]).

# Terminal 3 (c3, malha conhece server):
erl -sname c3
     c(sensor).
     sensor:start(c3, [server]).

# Terminal 4 (c2, conhece c1,c3):
erl -sname c2
     c(sensor).
     sensor:start(c2, [c1,c3]).


sensor:stop(c1).
sensor:start(c1, [server,c2]).
